version: '3.8'

services:
    pwn_babystack:
        build: ./
        image: pwn_babystack
        container_name: pwn_babystack
        ports:
            - "8888:8888"
        restart: unless-stopped
